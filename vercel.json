{
  "buildCommand": "npm install --prefix backend && npm install --prefix frontend --include=dev && cd frontend && npx vite build && cd .. && mkdir -p public && cp -r frontend/dist/* public/",
  "rewrites": [
    {
      "source": "/((?!api/.*).*)",
      "destination": "/public/$1"
    },
    {
      "source": "/api/:path*",
      "destination": "/api/[...all]"
    }
  ]
}
